import React from 'react';
import Button from '../../components/common/Button';

const ReplyActionButtonsContainer = ({ ...rest }) => {
  return (
    <Button {...rest}>
      등록
    </Button>
  )
}

export default ReplyActionButtonsContainer;